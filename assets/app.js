const menuButton=document.getElementById("menuButton"),navLinks=document.getElementById("navLinks");if(menuButton&&navLinks){menuButton.addEventListener("click",()=>{const open=navLinks.classList.toggle("open");document.body.classList.toggle("menu-open",open);menuButton.setAttribute("aria-expanded",String(open));menuButton.textContent=open?"✕":"☰"});navLinks.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{navLinks.classList.remove("open");document.body.classList.remove("menu-open");menuButton.textContent="☰"}))}const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");obs.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll(".reveal").forEach(e=>obs.observe(e));const form=document.getElementById("joinForm");if(form){form.addEventListener("submit",e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form).entries()),entries=JSON.parse(localStorage.getItem("regenMovementRegistrations")||"[]");entries.push({...data,submittedAt:new Date().toISOString()});localStorage.setItem("regenMovementRegistrations",JSON.stringify(entries));document.getElementById("formStatus").textContent="Ihre Registrierung wurde aufgenommen. Das Organisationsteam wird sich bei Ihnen melden.";form.reset()})}