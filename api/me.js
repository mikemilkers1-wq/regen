const {requireUser}=require('../lib/auth');module.exports=async(req,res)=>{if(req.method!=='GET')return res.status(405).json({error:'Methode nicht erlaubt.'});try{const u=await requireUser(req,res);if(!u)return;res.json({user:{id:u.id,username:u.username,displayName:u.display_name,role:u.role}})}catch(err){console.error(err);res.status(500).json({error:'Sitzung konnte nicht geprüft werden.'})}};
