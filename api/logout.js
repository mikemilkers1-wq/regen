const {sql,ensureSchema,cookies,hashToken,clearSessionCookie,COOKIE}=require('../lib/auth');module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'Methode nicht erlaubt.'});try{await ensureSchema();const token=cookies(req)[COOKIE];if(token){const q=sql();await q`DELETE FROM campaign_sessions WHERE token_hash=${hashToken(token)}`}clearSessionCookie(res);res.json({ok:true})}catch(err){clearSessionCookie(res);res.json({ok:true})}};
